# Google Cloud Environment Configuration
# Copy this file to .env and update with your values for local development
# For production, these will be set as environment variables in Cloud Run

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-google_cloud_project
GOOGLE_CLOUD_REGION=your-google_cloud_region

# Application Configuration
NODE_ENV=your-node_env
PORT=your-port
HOST=your-host
ATLAS_URI=your-atlas_uri
DBNAME=your-dbname

# API Configuration
API_YAML_FILE=your-api_yaml_file
CORS_ORIGINS=your-cors_origins

# Session Configuration
COOKIE_EXPIRESIN=your-cookie_expiresin

# GitHub Configuration (optional)
# GH_AUTH_TOKEN=your-# gh_auth_token
# GH_USERNAME=your-# gh_username
# GH_REPO=your-# gh_repo
